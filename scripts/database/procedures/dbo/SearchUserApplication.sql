CREATE OR ALTER PROCEDURE [dbo].[SearchUserApplication]
(
    @SkippedCount BIGINT,
    @TakenCount BIGINT,
    @SkippedUserIds [dbo].[IntType] READONLY,
    @SkippedApplicationIDs [dbo].[SmallIntType] READONLY
) AS
BEGIN

END
GO